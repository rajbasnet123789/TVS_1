listener 1883 0.0.0.0
# Anonymous allowed — MQTT is reserved for future use.
# No code currently publishes or subscribes (see AGENTS.md).
# Add password_file + allow_anonymous false when MQTT is actively used.
allow_anonymous true
max_connections 100
persistence true
persistence_location /mosquitto/data
log_dest stdout
log_type error
log_type warning
log_type notice
log_type information
connection_messages true
